package com.ccdevx.spring_security_auth.enums;

/**
 * Enumeration defining the types of tokens used for authentication.
 */
public enum TokenType {
    BEARER; // Used to indicate an access token in the HTTP Authorization header.
}
